'use client';import {useState} from "react";
export default function ProjectCopilot({project}:{project:any}){const [q,setQ]=useState("");const [a,setA]=useState("");const [loading,setLoading]=useState(false);async function ask(){if(!q.trim())return;setLoading(true);const r=await fetch("/api/ai/project-copilot",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({projectId:project.id,question:q})});const d=await r.json();setA(d.answer||d.error);setLoading(false)}return <div style={{background:"#fff",border:"1px solid #e6e8ef",borderRadius:14,padding:18}}><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Example: What needs my attention this week?" style={{width:"100%",minHeight:110,padding:12}}/><button onClick={ask} disabled={loading} style={{marginTop:8}}>{loading?"Thinking…":"Ask DeliverIQ ✨"}</button>{a&&<div style={{whiteSpace:"pre-wrap",marginTop:18,lineHeight:1.55}}>{a}</div>}</div>}
