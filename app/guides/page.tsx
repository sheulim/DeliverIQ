'use client';import {useMemo,useState} from "react";import {practitionerGuides} from "@/lib/content/practitionerGuides";
export default function Guides(){const [role,setRole]=useState("All"),[q,setQ]=useState(""),[open,setOpen]=useState<any>(null);const roles=["All",...Array.from(new Set(practitionerGuides.map((g:any)=>g.role)))];const shown=useMemo(()=>practitionerGuides.filter((g:any)=>(role==="All"||g.role===role)&&(!q||`${g.title} ${g.summary}`.toLowerCase().includes(q.toLowerCase()))),[role,q]);return <main style={{maxWidth:1280,margin:"0 auto",padding:32}}><section style={{padding:32,borderRadius:22,background:"linear-gradient(135deg,#101828,#312e81)",color:"white"}}><small>DELIVERIQ PRACTITIONER LIBRARY</small><h1 style={{fontSize:42,margin:"8px 0"}}>{practitionerGuides.length}+ Playbooks & AI-era Guides</h1><p>Learn → Apply → Download → Ask AI → Automate → Measure.</p></section><div style={{display:"grid",gridTemplateColumns:"1fr 2fr",gap:10,margin:"18px 0"}}><select value={role} onChange={e=>setRole(e.target.value)}>{roles.map(r=><option key={r}>{r}</option>)}</select><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search guides..."/></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(275px,1fr))",gap:12}}>{shown.map((g:any)=><article key={`${g.role}-${g.title}`} style={{border:"1px solid #e4e7ec",borderRadius:14,padding:18,background:"white"}}><small>{g.role}</small><h2>{g.title}</h2><p>{g.summary}</p><button onClick={()=>setOpen(g)}>Open full guide →</button></article>)}</div>{open&&<div onClick={()=>setOpen(null)} style={{position:"fixed",inset:0,background:"#10182899",display:"grid",placeItems:"center",padding:20,zIndex:99}}><article onClick={e=>e.stopPropagation()} style={{maxWidth:900,width:"100%",maxHeight:"88vh",overflow:"auto",background:"white",padding:28,borderRadius:18}}><small>{open.role}</small><h1>{open.title}</h1><p>{open.summary}</p><p><b>{open.journey.join(" → ")}</b></p>{open.sections.map((s:any)=><section key={s.title}><h2>{s.title}</h2><ul>{s.items.map((x:string)=><li key={x}>{x}</li>)}</ul></section>)}<h2>Ask Agile Assistant</h2>{open.prompts.map((p:string)=><blockquote key={p} style={{background:"#f2f4f7",padding:12,borderRadius:10}}>{p}</blockquote>)}<button onClick={()=>setOpen(null)}>Close</button></article></div>}</main>}
