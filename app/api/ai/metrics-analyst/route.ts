import {NextResponse} from "next/server";import OpenAI from "openai";import {metricsCatalog} from "@/lib/content/metricsCatalog";
export async function POST(req:Request){const b=await req.json();if(!process.env.OPENAI_API_KEY)return NextResponse.json({answer:"Configure OPENAI_API_KEY for live analysis."});const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await client.responses.create({model:process.env.OPENAI_MODEL||"gpt-4.1-mini",input:[{role:"system",content:`You are DeliverIQ Metrics Analyst. Use supplied evidence for project claims. Separate facts, interpretation, missing evidence and recommendations. Never infer individual performance and never claim causation merely from correlation. Catalogue: ${JSON.stringify(metricsCatalog)}`},{role:"user",content:`Question: ${b.question||""}\nEvidence: ${JSON.stringify(b.evidence||{})}`} ]});return NextResponse.json({answer:r.output_text});}
