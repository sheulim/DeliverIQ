import {createClient} from "@/lib/supabase/server";import Link from "next/link";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:p}=await sb.from("projects").select("*").eq("id",id).single();if(!p)return <main style={{padding:32}}>Project not found.</main>;const {data:s=[]}=await sb.from("stakeholders").select("*").eq("project_id",id);return <main style={{padding:32,maxWidth:1200,margin:"0 auto"}}><Link href={"/projects/"+id}>← Project</Link><h1>Stakeholders & RACI</h1><p>Map engagement and clarify decision/accountability roles in one view.</p><table style={{width:"100%",background:"#fff",borderCollapse:"collapse"}}><thead><tr><th>Stakeholder</th><th>Influence</th><th>Interest</th><th>R</th><th>A</th><th>C</th><th>I</th></tr></thead><tbody>{s.map((x:any)=><tr key={x.id}><td>{x.name}</td><td>{x.influence}</td><td>{x.interest}</td><td>—</td><td>—</td><td>—</td><td>—</td></tr>)}</tbody></table></main>}
