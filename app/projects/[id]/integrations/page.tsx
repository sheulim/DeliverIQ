import {createClient} from "@/lib/supabase/server";import Link from "next/link";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:project}=await sb.from("projects").select("*").eq("id",id).single();if(!project)return <main style={{padding:32}}>Project not found.</main>;return <main style={{padding:32,maxWidth:1100,margin:"0 auto"}}><Link href={"/projects/"+id}>← Project</Link><h1>Delivery Data Hub</h1><p>Bring external delivery data into DeliverIQ without turning DeliverIQ into Jira.</p><section style={{background:"#fff",border:"1px solid #e6e8ef",padding:18,borderRadius:14}}><h2>Sources</h2><p>CSV import: ready architecture</p><p>Jira: connector-ready placeholder</p><p>Azure DevOps: connector-ready placeholder</p><p>Future: Microsoft Project / Smartsheet / Monday / Asana</p></section></main>}
