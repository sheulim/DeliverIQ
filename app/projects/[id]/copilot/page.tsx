import {createClient} from "@/lib/supabase/server";import Link from "next/link";import ProjectCopilot from "@/components/ProjectCopilot";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:p}=await sb.from("projects").select("*").eq("id",id).single();if(!p)return <main style={{padding:32}}>Project not found.</main>;return <main style={{padding:32,maxWidth:1000,margin:"0 auto"}}><Link href={"/projects/"+id}>← Project</Link><h1>AI PM Workspace</h1><p>Ask DeliverIQ anything about {p.name}.</p><ProjectCopilot project={p}/></main>}
